services:
  msc_node:
    build: .
    container_name: msc_node

    ports:
      - "21:21"
      - "21100-21110:21100-21110"

    volumes:
      - ./data:/data

    networks:
      telecom-network:
        ipv4_address: 172.30.0.10

networks:
  telecom-network:
    external: true
